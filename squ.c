#include<stdio.h>
#include<conio.h>
int main()
{
int base,exponent;
long long power=1;
int i;
printf("enter the base");
scanf("%d",&n);
printf("enter the exponent");
scnanf("%d",&n);
for(i=1;i<=exponent;i++)
pow=pow*base;
printf("base,exponent");
return 0;
getch();
}
